apiVersion: v1
kind: Pod
metadata:
  name: {{ .Release.Name }}-test
  annotations:
    helm.sh/hook: test
spec:
  containers:
    - name: test
      image: {{ .Values.image }}
      command: ["sh", "-c", "echo test"]
  restartPolicy: Never
